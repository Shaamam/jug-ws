package tools.muthuishere.todo.todo;

// TODO: Add necessary imports during the workshop
// import org.springframework.data.jpa.repository.JpaRepository;
// import org.springframework.stereotype.Repository;
// import tools.muthuishere.todo.todo.model.Todo;

/**
 * TODO: Create the TodoRepository interface during the workshop.
 * This will extend JpaRepository to provide basic CRUD operations.
 * 
 * Workshop tasks:
 * 1. Make this interface extend JpaRepository<Todo, Long>
 * 2. Add @Repository annotation
 * 3. Add custom query methods if needed
 */
public interface TodoRepository {
    
    // TODO: Extend JpaRepository<Todo, Long>
    
    // TODO: Add custom query methods if needed (e.g., findByCompleted)
    
}